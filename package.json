{
  "name": "regex-grammar",
  "version": "1.0.0",
  "description": "",
  "main": "grammar.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:grammar": "nodemon --ext ne --exec 'nearleyc grammar.ne -o output/grammar.js && nearley-railroad grammar.ne -o output/index.html'",
    "watch:railroad": "lite-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nearley": "^2.19.7"
  },
  "devDependencies": {
    "lite-server": "^2.5.4",
    "nodemon": "^2.0.4"
  }
}
